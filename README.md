<!DOCTYPE html> <html lang="pt-BR"> <head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <title>Painel de Chamadas Profissional</title> <style> * { box-sizing: border-box; margin: 0; padding: 0; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; } body { background-color: #0f172a; color: #f8fafc; height: 100vh; display: flex; flex-direction: column; overflow: hidden; } header { background: linear-gradient(90deg, #1e3a8a, #3b82f6); color: white; padding: 15px 30px; text-align: center; font-size: 24px; font-weight: bold; letter-spacing: 1px; box-shadow: 0 4px 10px rgba(0,0,0,0.3); } .container { display: flex; flex: 1; height: calc(100vh - 64px); overflow: hidden; } /* LAYOUT DA TV (DIVISÃO DE TELA) */ .tv-esquerda { flex: 4; display: flex; flex-direction: column; background-color: #1e293b; border-right: 4px solid #334155; } .tv-direita { flex: 6; display: flex; flex-direction: column; background-color: #000; } /* Painel de Chamada Principal */ .chamada-box { flex: 1; display: flex; flex-direction: column; justify-content: center; align-items: center; padding: 20px; text-align: center; } .chamada-titulo { font-size: 2.2rem; color: #94a3b8; font-weight: 600; text-transform: uppercase; letter-spacing: 2px; } .senha-atual { font-size: 8rem; color: #38bdf8; font-weight: 900; margin: 10px 0; text-shadow: 0 4px 20px rgba(56,189,248,0.2); } .sala-destino { font-size: 3.5rem; color: #4ade80; font-weight: 700; background: rgba(74,222,128,0.1); padding: 10px 40px; border-radius: 50px; border: 2px solid #22c55e; } /* Histórico Lateral Inferior */ .historico-box { height: 250px; background-color: #0f172a; padding: 15px; border-top: 4px solid #334155; } .historico-box h3 { font-size: 1.2rem; color: #94a3b8; margin-bottom: 10px; border-bottom: 2px solid #1e293b; padding-bottom: 5px; } .historico-item { display: flex; justify-content: space-between; padding: 10px 15px; background: #1e293b; margin-bottom: 6px; border-radius: 6px; font-size: 1.2rem; } .historico-item:first-child { background: #1e3a8a; border-left: 5px solid #38bdf8; color: #fff; font-weight: bold; } /* Área de Vídeo */ .video-container { width: 100%; height: 100%; position: relative; } .video-container iframe { width: 100%; height: 100%; border: none; } /* PAINEL DO MÉDICO */ .controle-medico { max-width: 500px; margin: 40px auto; background: #1e293b; padding: 30px; border-radius: 12px; box-shadow: 0 10px 25px rgba(0,0,0,0.5); width: 95%; } .controle-medico h2 { margin-bottom: 20px; color: #fff; text-align: center; border-bottom: 2px solid #334155; padding-bottom: 10px; } label { display: block; margin-bottom: 8px; color: #94a3b8; font-size: 1rem; } select { width: 100%; padding: 15px; margin-bottom: 20px; border: 2px solid #334155; background: #0f172a; color: white; border-radius: 8px; font-size: 1.1rem; } .btn-grupo { display: grid; grid-template-columns: 1fr 1fr; gap: 15px; margin-bottom: 20px; } .btn-call { padding: 20px 10px; border: none; border-radius: 8px; font-size: 1.2rem; font-weight: bold; cursor: pointer; color: white; transition: transform 0.1s; text-align: center; } .btn-call:active { transform: scale(0.98); } .btn-geral { background-color: #2563eb; } .btn-preferencial { background-color: #d97706; } .btn-chamar-novamente { background-color: #4b5563; grid-column: span 2; padding: 12px; font-size: 1rem; } .hidden { display: none !important; } </style> </head> <body> <header>CENTRO MÉDICO - SISTEMA DE ATENDIMENTO</header> <!--   VISÃO DA TV DA RECEPÇÃO --> <div id="tela-tv" class="container hidden"> <div class="tv-esquerda"> <div class="chamada-box"> <div class="chamada-titulo">Senha Chamada</div> <div id="tv-senha" class="senha-atual">---</div> <div id="tv-sala" class="sala-destino">Aguardando...</div> </div> <div class="historico-box"> <h3>ÚLTIMAS CHAMADAS</h3> <div id="lista-historico"></div> </div> </div> <div class="tv-direita"> <div class="video-container"> <!-- SUBSTITUA O ID DO VÍDEO ABAIXO SE QUISER (Mantenha o ?autoplay=1&mute=1) --> <iframe src="https://youtube.com" allow="autoplay; encrypted-media" allowfullscreen></iframe> </div> </div> </div> <!--   VISÃO DO PAINEL DO MÉDICO --> <div id="tela-medico" class="controle-medico hidden"> <h2>Painel do Consultório</h2> <label for="medico-sala">Selecione seu Consultório:</label> <select id="medico-sala"> <option value="Consultório 1">Consultório 1 (Dr. 01)</option> <option value="Consultório 2">Consultório 2 (Dr. 02)</option> <option value="Consultório 3">Consultório 3 (Dr. 03)</option> <option value="Consultório 4">Consultório 4 (Dr. 04)</option> <option value="Consultório 5">Consultório 5 (Dr. 05)</option> </select> <label>Chamar Próxima Senha:</label> <div class="btn-grupo"> <button class="btn-call btn-geral" onclick="proximaSenha('Geral')">GERAL</button> <button class="btn-call btn-preferencial" onclick="proximaSenha('Pref.')">PREFERENCIAL</button> <button class="btn-call btn-chamar-novamente" onclick="chamarNovamente()">Repetir Última Chamada</button> </div> </div> <script> const urlParams = new URLSearchParams(window.location.search); const modo = urlParams.get('modo'); // Inicializa contadores de senha caso não existam no banco de dados local do navegador if(!localStorage.getItem('num_geral')) localStorage.setItem('num_geral', '0'); if(!localStorage.getItem('num_pref')) localStorage.setItem('num_pref', '0'); if (modo === 'tv') { document.getElementById('tela-tv').classList.remove('hidden'); carregarDadosTV(); setInterval(carregarDadosTV, 1500); // Verifica novidades a cada 1.5s } else { document.getElementById('tela-medico').classList.remove('hidden'); } // Função executada pelo médico para gerar e chamar a próxima senha function proximaSenha(tipo) { const sala = document.getElementById('medico-sala').value; let chave = tipo === 'Geral' ? 'num_geral' : 'num_pref'; let proximoNumero = parseInt(localStorage.getItem(chave)) + 1; localStorage.setItem(chave, proximoNumero); // Formata o texto da senha (Ex: G-004 ou P-012) const prefixo = tipo === 'Geral' ? 'G' : 'P'; const senhaFormatada = `${prefixo}-${String(proximoNumero).padStart(3, '0')}`; registrarChamada(senhaFormatada, sala); } function chamarNovamente() { const sala = document.getElementById('medico-sala').value; let historico = JSON.parse(localStorage.getItem('chamadas_clinica')) || []; // Encontra a última chamada feita por esta sala const ultimaDaSala = historico.find(c => c.sala === sala); if(ultimaDaSala) { registrarChamada(ultimaDaSala.senha, sala); } else { alert("Nenhuma senha foi chamada por este consultório ainda."); } } function registrarChamada(senha, sala) { let historico = JSON.parse(localStorage.getItem('chamadas_clinica')) || []; const novaChamada = { senha: senha, sala: sala, hora: new Date().toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'}), id: Date.now() // ID único para forçar a TV a detectar como nova chamada }; historico.unshift(novaChamada); if (historico.length >
4) historico.pop(); // Mantém as 4 últimas no histórico localStorage.setItem('chamadas_clinica', JSON.stringify(historico)); alert(`Senha ${senha} chamada para o ${sala}!`); } // Lógica de Atualização e Áudio da TV let idUltimaChamada = ""; function carregarDadosTV() { const historico = JSON.parse(localStorage.getItem('chamadas_clinica')) || []; if (historico.length > 0) { const atual = historico[0]; if (idUltimaChamada !== atual.id) { document.getElementById('tv-senha').innerText = atual.senha; document.getElementById('tv-sala').innerText = atual.sala; falarChamada(atual.senha, atual.sala); idUltimaChamada = atual.id; } // Renderiza o histórico na tela da TV const listaHtml = historico.map(item => ` <div class="historico-item"> <span>Senha: ${item.senha}</span> <span>${item.sala} (${item.hora})</span> </div> `).join(''); document.getElementById('lista-historico').innerHTML = listaHtml; } } function falarChamada(senha, sala) { if ('speechSynthesis' in window) { window.speechSynthesis.cancel(); // Separa as letras para o robô soletrar melhor (Ex: "G 0 0 1" em vez de "Gis mil e um") const senhaLetraPorLetra
